# URL base para llamadas desde el frontend (axios/fetch)
# - Dev con proxy Vite: /api/v1
# - Sin proxy / despliegue separado: http://localhost:8080/api/v1 (o tu URL real)
VITE_API_BASE_URL=/api/v1

# Target del proxy de Vite (solo en desarrollo `npm run dev`)
# Si tu backend no corre en :8080, cámbialo aquí.
VITE_API_PROXY_TARGET=http://localhost:8080
